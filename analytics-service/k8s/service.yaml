apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: togglemaster
  labels:
    app: analytics-service
spec:
  type: LoadBalancer
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 80
      targetPort: 8005
      protocol: TCP
