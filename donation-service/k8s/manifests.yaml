apiVersion: apps/v1
kind: Deployment
metadata:
  name: donation-service
  namespace: togglemaster
  labels:
    app: donation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: donation-service
  template:
    metadata:
      labels:
        app: donation-service
    spec:
      imagePullSecrets:
      - name: ocir-secret
      initContainers:
      - name: wait-for-postgres
        image: docker.io/library/busybox:1.36
        command: ['sh', '-c', 'until nc -z postgres.togglemaster.svc.cluster.local 5432; do echo waiting for postgres; sleep 2; done']
      containers:
      - name: donation-service
        image: sa-saopaulo-1.ocir.io/grqkmwwimskh/hackathon-repo/donation-service:latest
        ports:
        - containerPort: 8082
        env:
        - name: PORT
          value: "8082"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: donation-service-secret
              key: DATABASE_URL
        - name: OCI_QUEUE_ID
          valueFrom:
            secretKeyRef:
              name: donation-service-secret
              key: OCI_QUEUE_ID
        - name: OCI_QUEUE_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: donation-service-secret
              key: OCI_QUEUE_ENDPOINT
        # OpenTelemetry / New Relic
        - name: OTEL_SERVICE_NAME
          value: "donation-service"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "https://otlp.nr-data.net"
        - name: NEW_RELIC_LICENSE_KEY
          valueFrom:
            secretKeyRef:
              name: newrelic-secret
              key: license-key
        - name: OTEL_EXPORTER_OTLP_HEADERS
          value: "api-key=$(NEW_RELIC_LICENSE_KEY)"
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: donation-service
  namespace: togglemaster
spec:
  selector:
    app: donation-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8082
---
apiVersion: v1
kind: Secret
metadata:
  name: donation-service-secret
  namespace: togglemaster
type: Opaque
stringData:
  DATABASE_URL: "postgres://togglemaster_user:togglepass123@postgres.togglemaster.svc.cluster.local:5432/donation_db"
  OCI_QUEUE_ID: "SUBSTITUIR_PELO_OCID_DA_QUEUE"
  OCI_QUEUE_ENDPOINT: "SUBSTITUIR_PELO_ENDPOINT_DA_QUEUE"
